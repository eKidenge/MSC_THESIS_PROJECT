services:
  - type: web
    name: MSC_THESIS_PROJECT
    env: python
    pythonVersion: 3.12.17
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn thesis.wsgi:application
